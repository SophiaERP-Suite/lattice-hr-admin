
export interface CountryData {
  countryId: number;
  name: string;
  code: string;
}

export interface StateData {
  stateId: number;
  name: string;
  code: string;
}

export interface CityData {
  cityId: number;
  name: string;
  code: string;
}
